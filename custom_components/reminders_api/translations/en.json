{
  "config": {
    "step": {
      "user": {
        "title": "Set up Reminders API integration",
        "description": "Configure your Reminders API server connection",
        "data": {
          "name": "Name",
          "url": "API Server URL",
          "base_path": "Base Path (e.g. /api, leave empty for root)",
          "token": "API Token (optional)"
        },
        "data_description": {
          "name": "A friendly name for this integration",
          "url": "The URL of your reminders server (e.g. https://reminders.example.com)",
          "base_path": "Optional base path if using path-based routing (e.g. /api). Leave empty if API is at root.",
          "token": "Optional authentication token"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the API server",
      "invalid_auth": "Invalid authentication token",
      "unknown": "Unexpected error occurred"
    },
    "abort": {
      "already_configured": "This API server is already configured"
    }
  }
}
